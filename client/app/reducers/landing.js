// import actionTypes from '../constants'
const initialState = {
  name: ''
}
export default function landing (state = initialState, action) {
  const { type } = action

  switch (type) {
    default:
      return state
  }
}
