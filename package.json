{
  "name": "govau-transitions-beta",
  "version": "0.0.1",
  "engines": {
    "node": "5.10.0",
    "npm": "3.5.0"
  },
  "scripts": {
    "postinstall": "node client/ui-kit && cd client && npm install",
    "rails-server": "echo 'visit http://localhost:3000/hello_world' && foreman start -f Procfile.dev",
    "test": "rspec"
  },
  "devDependencies": {
    "download-file": "^0.1.5"
  }
}
